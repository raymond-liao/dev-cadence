# Dev Cadence Config

## Output Language

`output_language`: `en`

Allowed values:

- `en`: Write workflow documents and records in English.
- `zh-CN`: Write workflow documents and records in Simplified Chinese.

If this file is missing or the value is unsupported, default to `en`.
