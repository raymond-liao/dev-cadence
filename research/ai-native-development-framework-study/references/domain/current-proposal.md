# Current Proposal

The current proposal is maintained at the repository root:

```text
README.md
```

During execution, Agents should read the root `README.md` as the authoritative current version of the AI Native Development Framework proposal.

When citing the current proposal, use source ID:

```text
SRC-PROPOSAL-README
```

Agents must not rewrite the root `README.md` unless the user explicitly asks for proposal revisions to be applied.

